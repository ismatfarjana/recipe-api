<main>
  {{#if user }}
    <h1 id="title">Welcome to homepage</h1>
  {{else}}
    <h1 id="title">Welcome.  Please <a class="auth" href="/register">Register</a> and <a class="auth" href="/login">Login</a> to create your Profile</h1>
  {{/if}}  
  {{#if user }}
    <div class="welcome_part button">
      <a href="/logout">Logout</a>
    </div>
  {{else}}  
    <div class="welcome_part button">
      <a href="/login">Login</a>
    </div>
    <div class="welcome_part button">
      <a href="/register">Register</a>
    </div>
  {{/if}}
</main>
  
